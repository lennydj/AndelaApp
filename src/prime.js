function getPrimes(n) {
	primeArray = [];
	for (var x = 0; x<= n; x++) {
		for (var i = 2; i <= n/2; i++){
			if (i % n === 0) {
				primeArray.push[i];
				
			}
			else {
				return i + " is not a prime number";
			}
		return primeaArray;
		}
	}
}

getPrimes(20);